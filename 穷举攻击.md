---
title: 穷举攻击
tags:
  - 密码学
  - 密码分析
  - 通用攻击
---

# 穷举攻击

## 基本思想

敌手获得一个明密文对 $(P^*, C^*)$。由于密码算法是公开的，所以可以对密钥进行枚举。

注意到一种思想就是以结果来分析原因

## 单个明密文对

考虑穷举所有密钥。对于任意一个密钥 $k$，分析概率：

$$
Pr(Enc_k(P^*) = C^*)
$$

- 当 $k$ 是正确密钥时，这个概率是 1。
- 当 $k$ 是错误密钥时，这个概率是 $\frac{1}{2^n}$，其中 $C$ 是 $n$ bit 长的密文。

为什么概率不是 0？因为当 $k$ 随机变化的时候，可以认为生成的密文也是在密文空间中随机分布的。

本质就是概率不同，我们可以用统计学的方法，把这些事件区分开来

具体分析，假设分组长度为64bit，密钥长度为80bit
那么当我们穷举所有密钥时，应该能找到

$$
2^{80}\times \frac{1}{2^{64}}= 2^{16}
$$

个候选密钥
严谨来说，应该是

$$
(2^{80}-1)\times \frac{1}{2^{64}}+1
$$

不过取个整之后是一样的

## 从候选密钥中筛选出唯一正确的密钥

再获取一个明密文对 $(P', C')$。

考虑概率

$$
Pr(Enc_k(P^*)= C^* \wedge Enc_k(P')=C')
$$

- 正确密钥的概率是 1。
- 错误密钥的概率仍然是 $\frac{1}{2^{64}}$。

此时没被筛掉的错误密钥期望有：

$$
2^{16} \times \frac{1}{2^{64}}=2^{-48}
$$

## 存储上的改进

上面这种筛法需要存储 $2^{16}$ 个候选密钥。不过如果第一次就以

$$
Pr(Enc_k(P^*)= C^* \wedge Enc_k(P')=C')
$$

来筛选的话，就不需要存储密钥了

## 计算资源

- 时间：$2^{80}+2^{16}$ 次加密。
- 存储：两个明密文对。

## 破解与破译

这种算法可以给出密码算法的安全上界
如果我们可以找到一种算法的复杂度小于这个上界，那么可以说破解了这个算法
但是不能说是破译了这个算法

## 相关笔记

- [计算安全](计算安全.md)
- [攻击思想](攻击思想.md)
