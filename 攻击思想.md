---
title: 攻击思想
tags:
  - 密码学
  - 密码分析
  - 攻击方法
---

# 攻击思想

## 区分

### 不随机现象

是一个可计算或者统计的指标
这个指标在具体密码算法下的分布与随机置换下的分布存在差异
就可以通过假设检验来区分

### 区分攻击（区分器）

利用不随机现象区分开随机置换

### 例子

（目的是进行区分，就是研究这个加密算法是真正的加密，还是只是随机置换）

假设有一个加密算法 $EG_2$，$P$、$C$、$K_0$、$K_1$ 均为 $n$ 比特，各密钥相互独立，$F$ 为置换。

假设我们发现了一个不随机现象$P\oplus C = 1$
但是对于随机置换$Pr(P\oplus C) = \frac{1}{2^n}$
所以我们有以下攻击

1. 计算 $x$ 个 $P_i \oplus C_i$。
2. 如果 $x$ 个值都等于 1，则输出为 $EG_2$ 算法。
3. 如果 $x$ 个值中至少有一个不等于 1，则输出为随机置换 $RP$。

我们来分析一下成功率

区分成功就意味着：当黑盒为 $EG_2$ 时输出 $EG_2$，当黑盒为随机置换时输出随机置换。

假设 $Pr(EG_2) = Pr(RP)=\frac{1}{2}$，那么：

$$
P(success) = Pr(黑盒是 EG_2 且输出 EG_2)+Pr(黑盒是 RP 且输出 RP)
$$

也就是

$$
\frac{1}{2}+\frac{1}{2}(1-(\frac{1}{2^n})^x)
$$

## 分割

### 密码算法的分割

将算法的各个部件分考虑，便于发现不随机现象

### 搜索空间的分割

利用区分器将必须整体搜索的大空间分割为可局部搜索的小空间，降低搜索空间大小，提高求解效率
将不随机现象转换为只与部分密钥比特有关的方程

### 密钥恢复攻击

实现密钥空间的分割，先恢复 $K_2$，再恢复 $K_0$、$K_1$。

找到一个d轮区分起，然后利用区分器实现对密钥的

> **待补充：** 这里原笔记未写完：需要补充“找到一个 d 轮区分器之后，如何利用区分器实现对密钥的恢复”。

## 相关笔记

- [密码分析学概述](密码分析学概述.md)
- [计算安全](计算安全.md)
- [对称加密算法](对称加密算法.md)
- [差分攻击](差分攻击.md)
- [线性分析](线性分析.md)
